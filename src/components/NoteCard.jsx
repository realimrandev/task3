import [link] from "react-router-dom";

export default function ({ note }) {
    return (
        <>
        link to={`/notes/${note._id}`} className=card bg-base-100 hover:shadow-lg transition-all duration-200 b        
        
        
        
        
        </>
